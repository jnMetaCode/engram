# API versioning policy

2026-05-06 — Decided: date-based API versions (like Stripe), pinned per
customer, with a 12-month deprecation window. Breaking changes ship behind a
new version date, never in place.
